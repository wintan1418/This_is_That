<nav class="fixed top-0 left-0 right-0 z-50 bg-slate-950/80 backdrop-blur-xl border-b border-teal-500/10">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <%= link_to root_path, class: "flex items-center gap-2 group" do %>
        <div class="w-10 h-10 bg-gradient-to-br from-teal-400 to-amber-400 rounded-xl flex items-center justify-center shadow-lg shadow-teal-500/25 group-hover:shadow-teal-500/40 transition-all">
          <span class="text-xl">🏠</span>
        </div>
        <span class="text-xl font-bold bg-gradient-to-r from-teal-300 to-amber-300 bg-clip-text text-transparent">
          This Is That
        </span>
      <% end %>

      <!-- Desktop Nav -->
      <div class="hidden md:flex items-center gap-6">
        <%= link_to "Find Match", search_path, class: "text-gray-300 hover:text-teal-300 transition-colors" %>
        <%= link_to "Explore", places_path, class: "text-gray-300 hover:text-teal-300 transition-colors" %>
        <%= link_to "About", about_path, class: "text-gray-300 hover:text-teal-300 transition-colors" %>
      </div>

      <!-- Auth Buttons -->
      <div class="flex items-center gap-4">
        <% if user_signed_in? %>
          <%= link_to dashboard_path, class: "flex items-center gap-2 text-gray-300 hover:text-white" do %>
            <div class="w-8 h-8 rounded-full bg-gradient-to-br from-teal-400 to-amber-400 flex items-center justify-center text-slate-900 font-semibold">
              <%= current_user.name&.first&.upcase || current_user.email.first.upcase %>
            </div>
            <span class="hidden sm:inline"><%= current_user.name || "Dashboard" %></span>
          <% end %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-4 py-2 text-sm text-gray-400 hover:text-white transition-colors" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-300 hover:text-teal-300 transition-colors" %>
          <%= link_to "Get Started", new_user_registration_path, class: "px-5 py-2.5 bg-gradient-to-r from-teal-500 to-amber-500 rounded-xl font-semibold text-slate-900 hover:opacity-90 transition-opacity shadow-lg shadow-teal-500/25" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
