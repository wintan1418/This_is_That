<nav class="theme-nav fixed top-0 left-0 right-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <%= link_to root_path, class: "flex items-center gap-2 group" do %>
        <div class="w-10 h-10 bg-gradient-to-br from-teal-500 to-amber-500 rounded-xl flex items-center justify-center shadow-lg group-hover:scale-105 transition-all">
          <span class="text-xl">🏠</span>
        </div>
        <span class="text-xl font-bold gradient-text">
          This Is That
        </span>
      <% end %>

      <!-- Desktop Nav -->
      <div class="hidden md:flex items-center gap-6">
        <%= link_to "Find Match", search_path, class: "text-theme-secondary hover:text-theme-accent transition-colors font-medium" %>
        <%= link_to "Explore", explore_path, class: "text-theme-secondary hover:text-theme-accent transition-colors font-medium" %>
        <%= link_to "About", about_path, class: "text-theme-secondary hover:text-theme-accent transition-colors font-medium" %>
      </div>

      <!-- Right Side Actions -->
      <div class="flex items-center gap-3">
        <!-- Theme Toggle -->
        <button onclick="document.querySelector('[data-controller=theme]').__stimulus_controller?.toggle() || toggleTheme()" 
                class="theme-toggle" 
                title="Toggle Theme">
          <span data-theme-icon>🌙</span>
        </button>
        
        <% if user_signed_in? %>
          <!-- Favorites Quick Link -->
          <%= link_to favorites_path, class: "p-2 rounded-lg bg-theme-card border border-transparent hover:border-yellow-500/30 text-theme-secondary hover:text-yellow-500 transition-all", title: "Favorites" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
            </svg>
          <% end %>
          
          <!-- Settings -->
          <%= link_to settings_path, class: "p-2 rounded-lg bg-theme-card border border-transparent hover:border-teal-500/30 text-theme-secondary hover:text-theme-primary transition-all", title: "Settings" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
          <% end %>
          
          <!-- Profile -->
          <%= link_to dashboard_path, class: "flex items-center gap-2 text-theme-secondary hover:text-theme-primary transition-colors" do %>
            <div class="w-9 h-9 rounded-full bg-gradient-to-br from-teal-500 to-amber-500 flex items-center justify-center text-white font-semibold shadow-md">
              <%= current_user.name&.first&.upcase || current_user.email.first.upcase %>
            </div>
            <span class="hidden sm:inline font-medium"><%= current_user.name || "Dashboard" %></span>
          <% end %>
          
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-4 py-2 text-sm text-theme-muted hover:text-theme-primary transition-colors" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-theme-secondary hover:text-theme-accent transition-colors font-medium" %>
          <%= link_to "Get Started", new_user_registration_path, class: "btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

<script>
  // Fallback theme toggle for when Stimulus isn't ready
  function toggleTheme() {
    const isDark = document.documentElement.classList.contains('dark-mode');
    if (isDark) {
      document.documentElement.classList.remove('dark-mode');
      document.documentElement.classList.add('light-mode');
      localStorage.setItem('theme', 'light');
    } else {
      document.documentElement.classList.add('dark-mode');
      document.documentElement.classList.remove('light-mode');
      localStorage.setItem('theme', 'dark');
    }
    document.querySelectorAll('[data-theme-icon]').forEach(icon => {
      icon.textContent = isDark ? '☀️' : '🌙';
    });
  }
  
  // Update icon on page load
  document.addEventListener('DOMContentLoaded', () => {
    const theme = localStorage.getItem('theme') || 'dark';
    document.querySelectorAll('[data-theme-icon]').forEach(icon => {
      icon.textContent = theme === 'dark' ? '🌙' : '☀️';
    });
  });
</script>
